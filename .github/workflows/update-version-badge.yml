name: Update Version Badge

on:
  push:
    branches:
      - master

jobs:
  update_version_badge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm install

      - name: Get version number
        id: version
        run: echo "::set-output name=version::$(node -p "require('./package.json').version")"

      - name: Update README.md
        run: |
          sed -i "s|!\\[Version\\].*|![Version](https://img.shields.io/badge/Version-${{ steps.version.outputs.version }}-blue.svg)|" README.md

      - name: Commit and push changes
        run: |
          git config --local user.email "samprince.franklink2020@vitstudent.ac.in"
          git config --local user.name "SamPrinceFranklin"
          git add README.md
          git commit -m "Update version badge"
          git push origin master
